# OS
.DS_Store

# Node
node_modules/
dist/
build/
.next/
.env
.env.*
!.env.example

# Logs
*.log

# pnpm
.pnpm-store/

# blog post drafts (generated by case-study-blog; docs/blog/manifest.json tracks state)
docs/blog/posts/

# Claude Code agent worktrees (isolation: "worktree" spawns) — local-only,
# never meant to be committed. Accidentally got committed once via a stray
# `git add -A` (2026-09-01) — this entry prevents a repeat.
.claude/worktrees/
